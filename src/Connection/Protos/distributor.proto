syntax = "proto2";

option csharp_namespace = "Connection.Proto.Distributor";

package proto;

service Distributor {
	rpc GetRoute (RoutePackage) returns (Route);
}

message Route {
	required bytes data = 1;
}

message RoutePackage {
	required bytes data = 1;
}
