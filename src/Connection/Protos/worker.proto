syntax = "proto2";

option csharp_namespace = "Connection.Proto.Worker";

package proto;

service Worker {
	rpc GetRouteSubgraph (ConnectedGraph) returns (Route);
}

message Route {
	required bytes data = 1;
}

message ConnectedGraph {
	required bytes data = 1;
}

